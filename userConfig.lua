
if Daneel == nil then
    Daneel = {}
end

Daneel.config = {
    -- list of the Scripts path
    -- to enabled dynamic getters and setters on these ScriptedBehaviors
    scriptPaths = {

    },


    -- Input
    input = {
        buttons = {

        }
    }
}
