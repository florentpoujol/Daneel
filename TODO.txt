config :
    notion d'environnement (develop, common, prod, ... )
    avec environement courant
    Daneel.config.environement.key ...

    object Daneel.Config.Get(key)  ou (environement.key)   global helper : config()

    Daneel.Config = {
        config = {
            default = {},
            common = {},
            env1 = {},
            env2 = {},
        }

        environement = ""
    }