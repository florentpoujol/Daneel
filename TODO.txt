
=================
Develop


- Object Daneel.Time
  avec propriétés 
  - time :RO temps en seconde depuis début jeu
  -timeSinceLevelLoad
  - deltaTime :RO duration of last frame

  - fixedTime :RO The time the latest FixedUpdate has started
  Fixed time is updated in regular intervals (equal to fixedDeltaTime) until time property is reached.
  - fixedDeltaTime : (can be set) The interval in seconds of all fixedUpdates 0.02sec = 50x/s

  - frameCount : nb of frames since begining of game
  - timeScale : 

  fonction FixedUpdate appelée tous les "fixedDeltaTime" 


- permettre de programmer un event pour un frame ou temps ultérieur  Event.FireAtFrame() Event.FireAtTime()


- trigger avec layer et fonction (OnTriggerEnter(gameObject))
    les triggers appartenant à un layer check against les gameObject du même layer
    un trigger et gameObject peu avoir plusieur layers (default layer : "default") peu appartenir à tous les layers, avec "all"


- garder notion de config par défaut dans fichier Daneel
  + permettre au dev d'écraser certaines clés + de définir ses clés
  Daneel.Config.default
  Daneel.Config.Get()
  user > 
  function Config()
      return {

      }
  end

  Config().key = value
  Daneel.Config.Get("")


- function gameObject = GameObject.NewFromScene(sceneNameOrAsset, params) -> retourne le gameObject qui est dans la scène au lieu de créer un GO avec le contenu de la scène dedant

- GameObject.Copy(gameObjectNameOrAsset) copiue un GO déjà présent dans la scène en reproduisant tous ses paramètres

- ajout/set de scriptedBehaviors avec gameObject.Sett > faire comme n'importe quel autre component (nomOrAlias = {})






==================
GUI 


ajouter color property à worldtext
wordWrap après label n'a pas d'effet
ajouter prefix aux label


Slider
    - scale de la progress bar affecte le handle


"progress bar" mais avec plusieurs éléments
> lorsque élément pas utilisé, il a opacity, scale etc au choix dans la config, donc
	out et in state  



input
	- param size qui limite la taille du champs mais pas du texte (flèches font se déplacer le texte)


