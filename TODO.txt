

- permettre l'appel de Behavior functions lors de l'appel aux Events

- check what happens when the transform is destroyed by component.destroy()

- allow to extends GetType pour retourner des types customs

Triggers
    - groupement de triggers et triggerable gameObjects
    - function OnTriggerEnter etc sont aussi appelée sur le trigger et recoivent le gameObject

- custom error message